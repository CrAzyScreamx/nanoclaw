{
  "name": "grocery-list-tools",
  "private": true,
  "type": "module",
  "description": "Dev-only manifest. Nothing here is built or installed at runtime: bun runs the .ts files directly and every import is a relative sibling. It exists so the documented typecheck (bun install && bunx tsc --noEmit -p tsconfig.json) can resolve Bun's globals.",
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.0"
  }
}
